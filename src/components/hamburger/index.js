import React from "react";

import styles from "./hamburger.module.scss"

export const Hamburger = ({src, alt}) => (
  <div className={styles.hamburger}>
    <div></div>
    <div></div>
    <div></div>
  </div>
);
